You will need automated builds. 
You will need the entry point(cheat.app) to be none obfuscated
In the Package.appxmanifest we need to automatically rename all overlay instances to a different string for each user